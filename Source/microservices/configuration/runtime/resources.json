{
    {{#each eventStoreForTenants}}
    "{{tenantId}}": {
        "eventStore": {
            "servers": [
                "{{server}}"
            ],
            "database": "{{database}}"
        }
    }
    {{/each}}
}